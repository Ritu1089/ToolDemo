---
# defaults file for myrole
# ansible-kafka-role/vars/main.yml

kafka_version: "3.5.2"
kafka_url: "https://archive.apache.org/dist/kafka/{{ kafka_version }}/kafka_2.13-{{ kafka_version }}.tgz"
kafka_user: "kafka"
kafka_group: "kafka"
kafka_install_dir: "/opt/kafka"
